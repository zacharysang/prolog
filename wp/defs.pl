/* 
 * This is a place to define core concepts of coding. This includes things like:
 *  - variables (has a type name and value)
 *  - literals (has a type and a value)
 *  - operators (has types for each operand)
 *  - expressions (combination of an operator with compatible operands)
 *  - types (has a name and a pattern)
 * and also includes weakest precondition-specific concepts like
 * expanding the weakest precondition, and 
 * 
 * 
*/



% Defining operators
assign(Var, Val, Condition) :-